This folder contains code for automating imports to and from Google Sheets and Plaque TV data.

Required dependencies:

```
    $ pip3 install git+https://github.com/jotform/jotform-api-python.git
```
